name: Install system deps
description: Install apt packages required to build lxml/kerberos Python extensions
runs:
  using: composite
  steps:
    - name: apt-get update && install
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y libxml2-dev libxslt-dev libkrb5-dev
